[install]
saveTextLockfile = true
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@xenwix.dev/vite-tanstack-config", "@xenwix.dev/mcp-js", "@xenwix.dev/vite-plugin-dev-server-bridge", "@xenwix.dev/vite-plugin-hmr-gate", "@xenwix.dev/email-js", "@xenwix.dev/webhooks-js"]
